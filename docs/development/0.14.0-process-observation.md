# 0.14.0 — approved single-process observation

Owner approved one selected process, CPU/working-set/private-commit/read-write I/O timelines, shared machine context, symptom markers and full HTML/JSON. Start from process details, not an unqualified historical PID. Baseline main: 893a6152a8ec7d1878d4cf2061dc1b6824542ddf. Branch: feature/0.14.0-process-observation.

Reuse the existing PerformanceSessionService scheduler, system source, markers and system graphs through composition, without changing their health semantics. Keep process observation modules separate and integrate a button into IncidentReviewForm. A session holds one limited-query/synchronize process handle, compares native creation time to the selected WMI creation timestamp at its microsecond precision, then never reopens/rebinds after terminal failure or exit. Capture native creation FILETIME and per-sample actual timestamps. No privilege adjustment, process changes or workload generation.

CPU = change in kernel+user 100ns time / actual monotonic interval / total active logical processors. WorkingSet includes shared resident pages; PrivateUsage is private committed memory, not resident RAM. I/O is process-accounted bytes, not physical-disk speed. First/missing/rollback/long-gap measurements stay unavailable; native zero stays zero. Hold gaps rather than connecting across missing data. System collection continues after process exit and uses separately recorded times on the same clock; no atomic-snapshot claim.

Defaults 120s/2s; choices 30/60/120/300/600s and 1/2/5s; no background monitoring after close. Source errors remain independently visible. Stop keeps completed pairs only; reports retain full raw and calculated evidence, selection/actor, markers and system context. New probe/repair commands, dependencies and workflow permission changes are out of scope.

Test-first contracts cover formula/precision, unavailable counters, identity changes, exit/cancel, shared-scheduler timing, report encoding and native/UI/export acceptance. Run existing Windows CI because current sandbox has no Windows/.NET. Finish documentation with implementation to avoid a redundant docs-only build. Final expected-head PR merge, fresh main/release/artifact checks still required. Live corporate Windows11/RDP/UAC/DPI and multi-group/affinity/CPU-hotplug scenarios remain pilot acceptance, not hosted test claims.

Primary API references: https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-getprocesstimes ; https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-getprocessiocounters ; https://learn.microsoft.com/en-us/windows/win32/api/psapi/nf-psapi-getprocessmemoryinfo ; https://learn.microsoft.com/en-us/windows/win32/api/psapi/ns-psapi-process_memory_counters_ex .
