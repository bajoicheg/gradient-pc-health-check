# 0.13.0 — Observed validation

## Baseline and RED

Baseline is released 0.12.0 / main `8445684c53265438c825c28ba4190b2f5d6c6013`. PR #34 initially contained only test contracts and stubs at `fb10c90afd027013bb9a986c632bd11171b03ac5`.

- [Initial Windows run 34704258080](https://github.com/bajoicheg/g-pc-health-check/actions/runs/34704258080), job 103581329481: clean compilation and prior suites, then 0/44 behavior plus 0/6 integration on absent implementation, exit 245.
- Review tests were committed as `2cc5e93c52a3fbb3897b4b0031fd751ca0baf09f` (tree `8df300bf13bdcc0be0aa3ad191404c71ee09de9c`). [Windows run 34707703169](https://github.com/bajoicheg/g-pc-health-check/actions/runs/34707703169), job 103590653001, tested merge `0bf40772eee917c058679c3ec8bd7e5a6867abf7`: 0 compiler warnings/errors, prior suites passed; 0/44 + 0/6 + 0/20 review cases on stubs. This observed RED is not a working release.

## Implementation GREEN

Full implementation and four review fixes were accepted through the normal connector write flow and committed as **0bcdddca0b9799a242d770da5ede837406341a6e**, tree **adbfeb9db933ba4a55cff4b5000a13f8a0cef99f**. Earlier write refusals no longer occurred in this attempt; no alternate write route or repository permission change was used.

[Windows run 34708083459](https://github.com/bajoicheg/g-pc-health-check/actions/runs/34708083459), job **103591685810**, tested merge **1f93f14f1cbd2c7aeeef851672430b70b4839c46**:

- Clean build: **0 compiler warnings, 0 compiler errors**. NuGet audit passed.
- Every previously enabled suite passed.
- **44/44 file-use behavior**, **6/6 native/UI/export integration**, **20/20 source-review acceptance**: 70 new cases passed.
- Published single-file EXE self-test passed.
- **32/32 portable EXE/worker tests** passed across four folder/name layouts, including Unicode, spaces and brackets.
- Exact FileVersion **0.13.0.0**, single-file packaging, checksum and pilot-manifest/UTF-8 gates passed. PR artifact uploads were intentionally skipped by the existing workflow.

The native fixture opens a dedicated synthetic file, verifies that Restart Manager reports the holding test process with matching creation time, preserves the handle and unchanged file bytes, closes only its own handle, and queries again. These are actual Windows API calls, not only synthetic source checks. UI checks construct the form/menu and test idle state/export, not visual DPI rendering. No real user file or repair was touched.

The four reviewed defects are addressed: invalid source records no longer cache a failed PID lookup; reserved DOS components are rejected; native cancellation 1223 is retained; service result counts cannot exceed capacity. The original fake provider was corrected to issue MORE_DATA before nonempty output, matching the native capacity protocol rather than weakening assertions.

## Finalization and limits

The documentation commit must receive fresh final PR checks before merge. Exact final head, fresh main build, Release, attestations and downloaded-file verification are recorded on [PR #34](https://github.com/bajoicheg/g-pc-health-check/pull/34), not inferred from the earlier GREEN run. PR EXE hashes are not release hashes.

Review was direct source/diff/documentation review, not an independent human/subagent review. Existing runtime changes outside the new feature are limited to Program test/menu wiring and project version metadata. Health thresholds, earlier tools, worker/IPC, context/Temp/UAC rules, unrestricted portable filenames/locations, dependency versions and workflow permissions were not changed.

Hosted tests use Windows Server 2025 and .NET SDK 8.0.425. Real Windows 11 ordinary/elevated/alternate-account/RDP, denied/protected-file/process, filesystem-provider and 100/150/200% DPI acceptance remains a pilot. Native cancellation is cooperative and RM calls may delay return. No interactive UAC, real DISM/SFC/DNS flush or user Temp cleanup was executed. The EXE remains without an Authenticode publisher signature.
