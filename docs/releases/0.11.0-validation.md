# 0.11.0 validation record

## Reproduced gaps and corrections

At test head `416181e8d02f6b61a8c24b763ad826db3cd5b09e`, Windows EXE run `34696411477` compiled without compiler warnings/errors; all previous suites and 30 context-policy cases passed. New integration was 2/7: the context UI, main clipboard and per-action HTML were missing, and native effective-role inspection failed.

The native issue was traced to requesting only TOKEN_QUERY on the current primary token. WindowsPrincipal.IsInRole duplicates a primary token at Identification level before CheckTokenMembership. Opening the current token with Query | Duplicate supplies the required access without applying a token to the process/thread or enabling privileges. Primary references:

- https://raw.githubusercontent.com/dotnet/runtime/v8.0.0/src/libraries/System.Security.Principal.Windows/src/System/Security/Principal/WindowsPrincipal.cs
- https://learn.microsoft.com/en-us/windows/win32/secauthz/access-rights-for-access-token-objects
- https://learn.microsoft.com/en-us/windows/win32/api/wtsapi32/nf-wtsapi32-wtsquerysessioninformationw

After UI/report/worker integration, head `2dbff628ae92931783e60cba6f31430aec22694e`, Windows run `34698835088`, tested merge `cab0a9bbd78ce345a69301cc3682980582aef09c`, again compiled with zero warnings/errors. Prior suites, 30/30 context and 7/7 integration cases passed. The newly added eight action-boundary tests reported 7/8: a regular file at the selected Temp-root path was incorrectly classified as a successful empty cleanup because Directory.Exists returned false. The process exited 242; publish/package tests were skipped.

The root check now reads attributes, distinguishes a directory from a regular file/reparse point, treats positively missing paths as no-op and retains access/read failures as failed actions. The test is unchanged; a fresh complete run is required before merge.

## Acceptance scope

There are 45 new cases: 30 context/preview, seven native/UI/serialization and eight action review cases. Actual final PR/main results and downloaded-byte provenance are recorded in PR #32 after execution. Earlier passing results must not be substituted for a later head's run.

Review is direct source/test review, not independent human/agent review. Synthetic fixtures prove the tested policy boundaries, read-only preview and non-mutating refusal, not a real standard-user/UAC credential transition. Native tests inspect the hosted Windows token/session. No live user Temp cleanup, DISM/SFC or DNS flush is executed. Real corporate Windows 11, alternate-account UAC, RDP/current-session profile resolution, redirected profile/Temp, missing WTS data, GUI/DPI and mixed repair acceptance remain pilot requirements. Authenticode remains absent.
